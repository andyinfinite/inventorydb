@page
@model InventoryDBRazor.Pages.maint.tooltypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ListToolType[0].ToolType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ListToolType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ToolType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TypeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TypeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
